export interface NotificationMenuProps {
  // Define any props here if needed
}

export interface NotificationMenuItem {
  text: string;
  color: 'success' | 'warning';
  // You can extend this interface with more properties if required
}